/**
 * 给你一个整数数组nums,请你将该数组升序排列。
 * 示例1：
 * 输入： nums = [5, 2, 3, 1];
 * 输出：[1,2,3,5]
 * 
 * 示例2：
 * 输入：nums = [5, 1, 1, 2, 0, 0];
 * 输出：[0, 0, 1, 1, 2, 5]
 * 
 * 提示：
 * 1 <= nums.length <= 5*10^4
 * -5 * 10^4 <= nums[i] <= 5*10^4
 */


/** 
 * 思路一：要不要返回的还是原来的数组， */
/**
 * @param {number[]} nums
 * @return {number[]}
 */

var sortArray = function(nums) {
    
}